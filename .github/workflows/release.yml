name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Read version
        id: version
        shell: pwsh
        run: |
          $v = python -c "from version import __version__; print(__version__)"
          echo "app_version=$v" >> $env:GITHUB_OUTPUT
          echo "App version: $v"

      - name: Build with PyInstaller
        run: pyinstaller --noconfirm ChatList.spec

      - name: Install Inno Setup
        run: choco install innosetup -y --no-progress

      - name: Build installer (Inno Setup)
        shell: pwsh
        run: |
          $Iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $Iscc)) {
            throw "Inno Setup not found: $Iscc"
          }
          & $Iscc "/DMyAppVersion=${{ steps.version.outputs.app_version }}" "ChatList.iss"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ChatList-Setup-${{ steps.version.outputs.app_version }}
          path: installer/ChatList-Setup-*.exe
          retention-days: 7

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: installer/ChatList-Setup-*.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
